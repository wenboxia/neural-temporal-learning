# TODO — Phase 5.5 重启计划（活文档）

> 入口文档见 [`start_prompt.md`](start_prompt.md)（**先读那个**）。
> **本文件是 Phase 5.5 的唯一活计划**：每完成一步、每次改计划，都直接改这里，
> 并在文末「进度日志」记一行。`phase4_plan.md` / `phase5_plan.md` 是已完成阶段的历史记录，不再更新。
>
> **计划创建**：2026-09-06 ｜ **最后更新**：2026-09-06
> **当前目标**：完成 KTH 硕士论文答辩（pass 即可，时间不紧张）

---

## 0. Context

冻结 TabPFN 之上的多时间尺度概念漂移适应。导师 2026-06-01 的诊断：
检测器在真实数据上不触发 ⇒ 三层结构从未被激活 ⇒ "方法无效" 这个结论从未被真正验证过。
要求重设计检测信号，争取正面结果；负面结果保留但降级，不作卖点。

**2026-09-06 重启，三方（Claude / Astra / Opus）交叉核实后确认的事实**：

1. **Insects 官方变点**（Souza 2020 Table 2, "Abrupt (bal.)", 52,848 instances）：
   **14,352 / 19,500 / 33,240 / 38,682 / 39,510**（温度变化 30°C → 20°C → ~35°C → …，即 P(X\|y) 漂移）。
   仓库原用的 12,672 / 14,256 / 17,952 / 46,728 / 52,008 是 50-chunk **P(y) 构成变化点**。
   实测类条件特征偏移：经验点 0.05–0.09（全流中位数 0.07）vs 官方点 0.25–0.49；
   且 12,672 与 14,256 都在同一段连续 class 5（[12,598, 14,352) 共 1,754 条）内部。
   **B1+ 四段按官方坐标只覆盖 2/5 个真实漂移**（early 含 14,352，mid 含 19,500）。
2. **检测器沉默的主因是实现层阈值**（✅ 已验证并修复，见 Step 1）。自写 ADWIN 用值域 Hoeffding 界、
   无经验方差，默认配置 200/200 切分要求 \|Δmean\| ≥ **0.209**，而真实 indicator 位移 ≈ 0.10。
3. **每个温度段末尾是一长串 class 5**（1754 / 74 / 97 / 537 / 822 条）；14,352 与 38,682 前 200 条 100% class 5。
   ⇒ TabPFN 探针的掉幅混有"类别消失-重现"效应，**不能用掉幅大小选任务**。
4. **class ID → 物种/性别无官方映射**（Souza 2020 与 USP 仓库均未提供）。
   任何二值化只能称 **ID 分组**，不能称 sex / species 分类。论文 Limitations 必写。
5. **文档已订正**（Step 2 完成）：γ 数字、9/15 计数、2×2 缺格、seed 未绑定 torch。
6. **检测延迟 145–447 步 ≈ TabPFN 滑窗自恢复时间（约 100–200 步）**
   ⇒ "报警后再校正" 的抢救窗口可能很小 ⇒ **必须先做判别性对照**（Step 5），
   不能直接上规模。检测器的评价指标是**延迟**，不只是"触不触发"。
7. **算力**：MacBook M2 Pro 只能 CPU（MPS 慢 5.6×，1.2 s/步）；ROG 幻 14（NVIDIA）2026-09-13 左右可用，
   预计 4–6×。**所有需互相比较的运行都在同一台机器上跑**（GPU/CPU 浮点结果有细微差别）。
8. **6 类改造成本**（7-agent 只读勘察 + 2 个 adversarial reviewer）：33 处 / 19 文件，含数学改动
   （概率向量重归一化、MSE 均值缩减致梯度缩 ~1/K、阈值 / lr / 检测器全部重标定、测试重写、
   与既有二值数字不可比），**45–60 工时 + ROG 重跑 12–15 h**。

---

## 1. 已定决策

| 决策 | 内容 |
|---|---|
| **三条创新点都做** | ① 对比信号检测器（导师路径 A）② 适应-遗忘双评价（导师维度 C，与路径 B 合并成前沿图）③ 诚实消融（四分支 oracle 对照） |
| **标签方案 = C（先二值后 6 类）** | 本周与第一批 ROG 实验用 pair 二值 `pair_A_vs_B`（{2,3} vs {4,5}）跑通检测-动作链；现有 {2,4,11}/{3,5,12} 更名 `pair_parity`。若检测-动作链有增益，再投 6 类原生作论文主数字 |
| **Git** | 每完成一项本地 commit 并推 main，显式列文件，绝不带私人转写 |
| **导师沟通** | 第一批结果出来后统一同步（段切错 + γ 更正 + river 结果一起讲） |
| **本周边界** | MacBook 只做编码与零成本诊断；长实验一律留给 ROG |

---

## 2. 第 1 周（2026-09-06，MacBook）— ✅ 全部完成

九个步骤全部落地，13 个 commit，**206 passed**，工作树干净。详细结果见 §9 进度日志。
一句话总结：**基础设施与诊断做完了，检测器已能触发，但"三层结构有没有用"仍未验证** ——
那要等 ROG 的批次二。

| Step | 交付 | 关键结果 |
|---|---|---|
| 1 检测器 | `RiverADWINDetector` + `--detector_impl` + 离线重放脚本 | 自写版 0/20 → **river 16/20 运行报警、官方漂移召回 1.00** |
| 2 校准 | `set_global_seed` + 坐标常量拆分 + 四份文档更正 | γ 数字、9/15 计数、2×2 缺格、seed 语义全部订正 |
| 5 动作策略 | `--action_on_alarm` × `--trigger_source` + 8 道守卫 | 判别性对照可跑 |
| 3 标签与切段 | `--label_scheme` + `--aligned_v2` + 驱动 `variant_tag` | 丢行后漂移坐标重映射；**段有效性守卫**（见 §5.2）|
| 4 对比信号 | `diag_contrast_signal.py` + 信号缓存 | 路径 A 成本≈0（批量 2–5 s vs 逐步 304–801 s）|
| 6 遗忘回测 | `src/utils/forgetting.py` | 留出集剔除 + state_hash 零污染断言 |
| 7 双记忆 | `DualMemoryLoader` | KDD 2026 文献基线；**实测确认均衡流上退化为滑窗** |
| 8 metrics | 共用恢复目标 + `errors_avoided` | 撤掉"各方法自己的门槛" |
| 9 ROG 手册 | [`ROG_RUNBOOK.md`](ROG_RUNBOOK.md) | 含 WSL2 安装、GPU 校准基准、五批次中止判据 |
| 补 | `--detector_input {indicator,pred1,contrast_*}` | 用户批准把 `pred1` 加进批次二 |

**本周唯一遗留**：`start_prompt.md` 的架构章节已更新模块表，但履历表（§4）仍是旧叙事，
等 ROG 出结果后连同论文骨架一起重写。

---

## 3. 第 2 阶段（ROG 幻 14）— 当前主线

**机器 2026-09-13 左右到手。** 完整命令、环境搭建、中止判据见 [`ROG_RUNBOOK.md`](ROG_RUNBOOK.md)。
每批跑完**停下汇报**再开下一批。

| 批次 | 内容 | ROG 估时 | 阻塞关系 |
|---|---|---|---|
| **0** | GPU 路径打通（`SlowPrior` 现硬编码 cpu）+ 校准对拍 | 2–3 h | 所有批次 |
| **1** | 新基线：Phase 1 + 双记忆，验 headroom | 2–4 h | 批次 2/3 |
| **2** | **判别性对照**（最关键）：oracle vs detector × 4 动作 × 4 检测输入 | 6–10 h | 决定论文主线 |
| **3** | 三层激活：phase4a vs phase1 vs 双记忆，3 seeds | 6–8 h | 批次 2 有增益才跑 |
| **4** | 适应-遗忘前沿图 | 8–12 h | **不阻塞**，可与 2/3 并行 |
| **5** | 收口：胜出配置补 5 seeds + paired t-test | 6–8 h | 批次 2–4 |

合计约 **30–45 GPU 小时**，含分析与汇报间隔，日历上约 **2 周**。

### 批次二的三种结局（决定后面怎么走）

| 结局 | 含义 | 下一步 |
|---|---|---|
| oracle 即时触发也无增益 | 问题在**动作**本身 | 优化信号救不了；论文主线转维度 C（遗忘）|
| oracle 有效、实际报警无效 | 瓶颈是**检测时机** | 路径 A 有意义，去压检测延迟 |
| 实际报警也有效 | 方法真的可行 | 扩规模补 seeds，这是最好的论文主线 |

### 本周（ROG 未到）可选：MacBook 预跑

批次一在 MacBook 上约 8–16 h。**它的数字不能直接用**（跨机器不可比），
但可以提前回答"新标签+新切段到底有没有 headroom"这个中止判据 ——
如果连 headroom 都没有，ROG 到手后要先改设计而不是直接开跑。
建议周末挂后台跑一次作 pilot。

---

## 3.5 时间线与导师要求对照

### 我们现在在哪

```
2026-06-01  导师汇报，给三个方向 ──┐
2026-06~09  停滞三个月（找实习）   │
2026-09-06  ★ 现在：基础设施完成，检测器已能触发
2026-09-13  ROG 到手 ─────────────┴──▶ 实验阶段开始
```

### 导师 2026-06-01 的要求，逐条对照

| # | 导师要求（原话要点）| 状态 | 依据 / 预计满足时间 |
|---|---|---|---|
| 1 | "让检测器触发得更多" | ✅ **已满足** | river ADWIN：Insects 16/20 运行报警、官方漂移召回 1.00；对照段零误报 |
| 2 | "检测器不触发，你这方法就无法验证" → 先让它能被验证 | ✅ **已满足** | 触发 + 四分支动作 + oracle 注入都已就绪，批次二即可跑 |
| 3 | 路径 A：适应前后差值作检测信号 | 🟡 **诊断完成，待规模验证** | 成本≈0 已证；信号强弱见 §5.1，**批次二** |
| 4 | 路径 B：提高 fixed_ratio 增强信号 | 🟡 **代码就绪，未跑** | `--context_loader composite`，**批次四** |
| 5 | 维度 C：适应-遗忘 trade-off（"最可能出正面结果"）| 🟡 **工具就绪，未跑** | `src/utils/forgetting.py`，**批次四** |
| 6 | **"得尽快定出来一版比较正面的结果"** | ❌ **未满足** ← 关键 | **批次二/四**，约 2026-09-25 |
| 7 | "直接在真实数据上验证" | ✅ 已满足 | 全部新实验都在 Insects 真实数据上 |
| 8 | 进度随时更新到 GitHub | ✅ 持续 | 13 commit 已推 |
| 9 | 开始写论文时告知导师，一起看 | ⏳ 待触发 | 预计 10 月初 |
| 10 | 答辩 PPT 先出草稿一起过 | ⏳ 待触发 | 预计 10 月中 |

**一句话**：导师提的"让检测器工作起来"已经做到了；**"拿到正面结果"还没有**，
那是批次二/四的事，最快约两周半后见分晓。

### 到答辩的时间线（现实版）

⚠️ 导师 6 月说的是"9 月底 10 月初答辩"，但项目停滞了三个月，那个时间点**已经不现实**。
导师同时说过 examiner "10 月底之前都比较好安排"，所以现实目标是 **10 月下旬答辩**。

| 时间 | 里程碑 | 判据 |
|---|---|---|
| 09-06 ~ 09-12 | 基础设施（✅ 完成）；可选 MacBook 预跑批次一 | — |
| 09-13 ~ 09-19 | ROG 批次 0/1/2 | **批次二出结论 → 论文主线定下来** |
| 09-20 ~ 09-26 | 批次 3/4 | 拿到正面结果（准确率增益 或 遗忘更少）|
| 09-27 ~ 10-03 | 批次 5 收口 + 整理数字与图 + **告知导师开始写作** | 满足导师要求 #6 |
| 10-04 ~ 10-17 | 论文起草（中文正文 + 英文摘要）；数据/方法章可提前写 | 初稿给导师 |
| 10-18 ~ 10-24 | 答辩 PPT 草稿 → 与导师对一版 → **答辩** | 满足 #9 #10 |
| 答辩后 1 周 | 提交论文 | 导师："答辩之后一周大概就要提交" |

**关键路径是批次二**。它一旦出结论，写作就能启动，因为数据与方法章节不依赖结果。
如果批次二三种结局都没拿到正面结果，退路是维度 C（批次四，不依赖检测器），
再不行就与导师讨论把负面结果按"方法探索"写，但那是最后手段。

**风险点**：ROG 上 `SlowPrior` 的 GPU 路径要先打通（现在硬编码 CPU）。
若 GPU 走不通，只能退回 CPU，全部批次时间 ×4，日历上要多两周，答辩要顺延到 11 月。
**这是目前最大的单点风险，ROG 到手第一天就要验证。**

---

## 3.6 最小可毕业路径（目标 = 答辩 pass，不追求完整）

**核心问题：跑到哪一步就够写论文了？** 答案是**批次二结束**，约 10–17 GPU 小时。

### 现在手上已经有的正面结果（不依赖任何 ROG 实验）

这三条都是本周做出来的、可验证的、能写进论文的：

1. **诊断出方法"失效"的真实原因并修好了**。检测器从不触发不是基础模型的性质，
   是自写 ADWIN 的阈值与信号量级不匹配（要求 0.209，真实信号 0.10）。
   换标准实现后官方漂移召回 1.00。**这本身就是一个正面结论**：
   "先前报告的失败是可修的实现问题，不是方法的固有局限"。
2. **检测输入的系统对照**：`pred1` 召回 2/3、`indicator` 仅 1/3，对照段零误报。
   给出了"在冻结表格基础模型上该喂检测器什么信号"的可复现建议。
3. **基准数据卫生的方法学发现**：官方漂移坐标此前用错；10 个段×标签组合有 3 个
   在漂移前是单类、不可用。这类问题会安静产出漂亮但无意义的数字。

⇒ **即便后续实验全是零增益，论文也有东西可写**，且是导师要的"正面"方向
（"是检测器把它拦下来了" 这个判断被证实）。

### 最小路径

| 阶段 | GPU 时 | 产出 | 够不够写论文 |
|---|---|---|---|
| 现在 | 0 | 上面三条 | 骨架够，缺主实验 |
| + 批次 0 | 2–3 | GPU 路径打通 | — |
| + 批次 1 | 2–4 | 新基线 + headroom 确认 | — |
| **+ 批次 2** | **6–10** | **"报警后动作值不值"的干净答案** | ✅ **够了，可以开写** |
| + 批次 4 | 8–12 | 适应-遗忘前沿图 | 更稳（导师点名的角度）|
| + 批次 3/5 | 12–16 | 多 seed 显著性 | 锦上添花 |

**最小 = 批次 0+1+2 ≈ 10–17 GPU 小时 ≈ 日历上 3–5 天。**
批次二无论出哪种结局都能写：
- 有增益 → 主线是"检测器修好后三层结构确实有效"
- 无增益 → 主线是"检测器可以修好，但修好后动作仍无增益"，
  这是**受控的**阴性结论（与 Phase 5 那种"根本没触发"完全不同），
  再补批次四的遗忘角度作正面点。

### 建议的取舍

- **必做**：批次 0、1、2。
- **强烈建议**：批次 4（不依赖检测器、导师点名、独立于批次二结果）。
- **暂缓（不是砍死）**：批次 3（与批次二重叠度高）、批次 5 的多 seed。
  ⚠️ 这只是**当前**的取舍，依据是"批次二还没跑、先省算力"。
  用户 2026-09-06 明确：等 ROG 实际跑起来、看过批次 0/1/2 的结果和实际速度后重新安排，
  **有可能全部批次都做**。若 ROG 比预期快，或批次二结果需要更强证据支撑，就把它们加回来。
- **明确不做**：6 类原生改造（45–60 工时）。

砍到最小后总量 **18–29 GPU 小时**，日历上一周内可完成，
之后进入写作，符合 10 月下旬答辩的时间线。

---

## 4. 验证方式

- `pytest tests/` 全绿（**当前 193 passed**）+ 每步新增单测。
- Step 1 的 river 重放数字可复现（mid 段官方点 local 3,500，延迟 145–172 步）。
- Step 4 出 A0 图与延迟表；**contrast 不优于 indicator 也如实记录**。
- Step 5–8 只跑 `--max_eval_steps 100` smoke，不在 MacBook 上跑长实验。

---

## 5.1 Step 4 结果：检测输入对照（2026-09-06，已按段有效性修正）

在**有效**的官方变点段上（`pair_A_vs_B`，river ADWIN δ=0.002，命中窗口 = 变点前 100 ~ 后 600 步）。
共 3 个有效漂移：d3_33240 一个，d4_double 两个。

| 检测输入 | 总召回 | 误报 | d3 延迟 | d4 延迟 | d3 位移 | d4 位移 |
|---|---|---|---|---|---|---|
| `pred1`（类先验）| **2/3** | 1 | −31 | **26** | **0.340** | **0.480** |
| `contrast_prob`（路径 A）| **2/3** | 1 | −45 | 68 | 0.028 | 0.278 |
| `contrast_hard`（路径 A）| **2/3** | 1 | −22 | 355 | 0.085 | 0.120 |
| `indicator`（现状）| **1/3** | 1 | **未检出** | 48 | 0.165 | 0.295 |

**结论**：
- **换掉 indicator 是对的**：三种替代信号都是 2/3，现状 indicator 只有 1/3。
- **`pred1` 是最佳候选**：召回并列第一、延迟最短、信号位移最大，
  而且**不需要第二路预测**（比路径 A 的对比信号更省）。已加为 `--detector_input pred1`。
- 路径 A 的对比信号可用但不占优，且 `contrast_hard` 在 d4 上延迟 355 步太慢。
- **无漂移的 `d0_control` 段上四种信号全部零报警** —— 检出不是靠放宽阈值换来的。
- 三个漂移没有一种信号能全检出（最好 2/3），**d4 的两个漂移相距仅 552 步，谁都只抓到一个**。

⚠️ **对 2026-09-06 早先版本的更正**：早先报的"contrast 2/2 vs indicator 1/2"包含
`d2_19500` + `pair_A_vs_B`，而那一段漂移前 567 行全是同一类，
stale 路退化成常量、"对比"根本不是对比。该行已作废，见 §5.2。

---

## 5.2 段有效性（2026-09-06 发现，影响切段设计）

漂移前后都必须有两个类别，否则测的是 P(y) 构成变化而不是概念漂移，
且单类 context 会让 TabPFN 走常量 fallback（漂移前准确率恒为 100%）。
实测 10 个 (段 × 标签方案) 组合中 **3 个退化**：

| 组合 | 问题 |
|---|---|
| `d1_14352` × 两种方案 | 漂移前 1252 行全是同一类 |
| `d2_19500` × `pair_A_vs_B` | 漂移前 567 行全是同一类 |

**根因是结构性的**：官方变点 14,352 正好落在一段 **1,754 条连续 class 5**
（[12,598, 14,352)）的末尾，所以任何含它的窗口漂移前必然单类 ——
那里温度漂移与标签构成变化**完全混淆**，无法用。

→ **可用漂移段只有 `d3_33240` 与 `d4_double`**（两种标签方案下都干净），
外加无漂移的 `d0_control` 测误报。
`load_insects()` 现在默认对退化组合**直接报错**并列出可用段，
`allow_degenerate=True` 是显式逃生口。这类问题会安静地产出漂亮但无意义的数字，所以必须硬失败。

⚠️ **对 ROG 批次的影响**：批次二/三的段列表要用 `d3_33240,d4_double,d0_control`，
不能用默认的全部五段。[`ROG_RUNBOOK.md`](ROG_RUNBOOK.md) 已同步。

---

## 5. 关键数字速查（含 Phase 5.5 更正）

| 指标 | 数值 |
|---|---|
| Phase 1 baseline（合成） | 79.89 ± 0.99% (regime_switching, n=5) |
| Phase 3 vs P1（合成） | rotating +1.00 sig / regime NS / combined −0.47 sig 负 |
| Oracle context-reset（合成） | +0.51 pp sig（context 污染只占漂移损失的 ~1/3） |
| Phase 4 detector 触发（合成，**计数已订正**） | raw 0/15、abs 0/15、indicator **9/15 运行 12 事件**、warmstart 同、fit05random 9/15 |
| Phase 5 Electricity phase4a vs P1 | −0.064 NS，detector 1/15 |
| Phase 5 Insects B1+ phase4a vs P1 | −0.172 sig p<0.0001，detector 0/20 |
| **自写 ADWIN 所需 \|Δmean\|**（默认配置 200/200 切分） | **0.209** ← 真实信号只有 0.10，结构性触发不了 |
| **river ADWIN 重放（δ=0.002）** | Insects **16/20 runs 报警、官方漂移 recall 1.00、中位延迟 266 步**；Electricity 1/15（渐进，应沉默） |
| **官方点 19,500 处 indicator 位移** | **0.095–0.105**（5/5 seed）← 旧文档的 "≤0.019、10× 稀释" 已作废 |
| **Insects 官方 drift 位置** | **14,352 / 19,500 / 33,240 / 38,682 / 39,510**（Souza 2020 Table 2） |
| Insects 经验 P(y) 变化点（旧用，非官方） | 12,672 / 14,256 / 17,952 / 46,728 / 52,008 |
| Insects B1+ 四段（旧切法，只覆盖 2/5 官方点） | early [10000,15000) / mid [16000,21000) / late_pre [42500,47500) / late_post [47848,52848) |
| TabPFN 探针在官方点 33,240 的掉幅 | pair_parity 100→83% ／ pair_A_vs_B 99→62% ／ 6 类 95→42%（**含类别重现效应，勿单独用于选任务**）|

---

## 6. 论文写作（阻塞于 §2–§3）

- [ ] 有结论后，和用户 + 导师一起定论文骨架
- [ ] 起草（中文正文 + 英文摘要）
- [ ] 答辩 PPT 草稿 → 导师说要先看一版再正式答辩

**已知写作约束**：目标是 pass，不是期刊 novelty；标题去 PFC 用 Multi-Timescale / Hierarchical；
Ch2.3 保留半页 PFC 灵感但明确不声称生物建模；Phase 2 / 2.5 进附录；
Phase 3 / 4 / 5 的负面结果保留但定位为方法演进的中间步骤；
Limitations 必写：① 合成 rotating +1pp 未迁移到真实数据 ② Insects 标签只是 ID 分组、无官方物种/性别映射
③ Phase 5 的段切法只覆盖 2/5 官方漂移 ④ 真实数据的多 seed 不是独立数据流。

> ⚠️ [`phase5_plan.md`](phase5_plan.md) §7 的 10 章大纲已过时，不要直接用。

---

## 7. 明确不做的事

| 不做 | 原因 |
|---|---|
| 加第三个真实数据集 | 导师认可现有两个（一渐进一突发）已够 |
| 现在就做 6 类改造 | 决策 C：先用二值验证检测-动作链有没有增益，有了再投 45–60 工时 |
| fit_threshold / init_strategy 等细粒度 ablation | 导师："治根不治本……应该是最后补充的实验，不应该是核心实验" |
| 投稿版论文 / 期刊投稿 | 目标已降级为硕士答辩 pass |
| 重跑 Phase 1–4 合成实验 | 数据已齐全且已 commit |
| 新 detector 设计先在合成数据上验证 | 导师：直接在真实数据上验证 |
| 改整体算法架构 | 导师：先确认方法可不可行，"如果完全不行，我觉得可以再看" |
| 在 MacBook 上跑长实验 | MPS 比 CPU 还慢 5.6×；等 ROG |
| `git add -A` / `git add .` | 会把导师私人录音转写推到**公开**仓库 |

---

## 8. 仓库卫生（延续项）

- [x] 导师录音转写加进 `.gitignore`（2026-09-05）
- [x] `.gitignore` 改动已提交（含 `advisor_demo_*.md`）
- [x] `results/smoke/` 与 `results/smoke_oracle.png` 已 gitignore
- [x] README 里 `run_phase4_a.py` 的 flag 修正 `--datasets` → `--dataset`
- [ ] `phase5_plan.md` §7.1 / §8 已过时（已加 superseded 标注，确定新骨架后可整段重写）

---

## 9. 进度日志

| 日期 | 步骤 | 结果 | commit |
|---|---|---|---|
| 2026-09-06 | Step 1 检测器 | river ADWIN 包装 + 零成本离线重放。**自写版 Insects 0/20 → river 16/20 运行报警、官方漂移 recall 1.00、中位延迟 266 步**；Electricity 仍 1/15。证实导师判断：是检测器把方法拦下来了。7 个新单测 | `bd07253` |
| 2026-09-06 | Step 2 校准 | 加 `set_global_seed` 到四个脚本；Insects 坐标常量拆成官方 / 经验两组并注明；四份文档加更正 banner（γ 数字、9/15、2×2 缺格、seed 语义）。115 passed | `2ff0ad7` |
| 2026-09-06 | 计划归位 | Phase 5.5 计划从对话搬进本文件，成为唯一活文档；Step 5 与 Step 3 顺序对调 | — |
| 2026-09-06 | Step 3 标签与切段 | `label_scheme {pair_parity, pair_A_vs_B}` + `aligned_v2` 官方变点居中 5 段（含无漂移对照段 d0_control）。**关键正确性点**：丢行后漂移坐标按保留行累计数重映射（d2 的 1500→767、d4 的 1082/1910→880/1432），否则 oracle 会触发在错样本上。驱动加 `variant_tag` 防 skip-existing 误跳、`--extra_args` 只传给认识的脚本。15 个新单测 | `ae62fc2` |
| 2026-09-06 | Step 6+4 遗忘/对比信号 | `src/utils/forgetting.py`（留出集剔除 + state_hash 零污染断言 + best-minus-final 遗忘定义）；`scripts/diag_contrast_signal.py`。17 个新单测 | `3327095` |
| 2026-09-06 | Step 7 双记忆 | `DualMemoryLoader`（类均衡淘汰 + 年龄上限）作 KDD 2026 文献基线；**实测确认退化性质**：类别均衡时与纯滑窗 context 完全相同，只在单类长段才分歧（单类段深处保有 25 条少数类样本，滑窗为 0）。14 个新单测 | `19d9316` |
| 2026-09-06 | Step 8 metrics | 共用恢复目标 A\* + errors_avoided（可从变点或**报警时刻**起算）；撤掉"各方法自己的门槛"。14 个新单测，193 passed | `18ee2d9` |
| 2026-09-06 | Step 4 最终 | 3 个**有效**漂移上：`pred1` / `contrast_*` 各 2/3，`indicator` 仅 1/3；`pred1` 延迟最短、位移最大且无需第二路预测 → **最佳检测输入**。对照段零误报 | `36992b2` |
| 2026-09-06 | 段有效性 | 10 个组合中 3 个退化（漂移前单类）；`d1_14352` 结构性不可用（官方点 14,352 落在 1,754 条单类段末尾）。可用段仅 `d3_33240` + `d4_double` | `36992b2` |
| 2026-09-06 | ~~Step 4 重评分~~ | 允许早报后结论变了：对比信号 **2/2 召回**、indicator 只有 **1/2**（漏 d3）；但延迟没压短（d2: 31–37 vs 18）。`d0_control` 零误报。`pred1` 同样 2/2 且 d3 位移最大，值得进批次二 | `(见下)` |
| 2026-09-06 | Step 9 ROG 手册 | [`ROG_RUNBOOK.md`](ROG_RUNBOOK.md)：MPS 实测比 CPU 慢 5.6× 的依据、WSL2 安装、GPU 校准基准（41s/0.9471）、五个批次含中止判据 | `25baf51` |
| 2026-09-06 | Step 5 ActionPolicy | `--action_on_alarm {route_adapter,context_reset,buffer_clear,none}` × `--trigger_source {detector,oracle}` + `--oracle_lag`。守卫全部落地：context_reset 在**预测前**生效（与 run_baselines oracle 同一时刻表）、最小长度 + 类别覆盖、空 oracle 报错、oracle 下 detector 转影子模式不被 clear、默认动作 auto-resolve 保 Phase 3 路径、`--consolidate_on_post_alarm_data` 可把巩固推迟到报警之后、输出 stem 带分支名。18 个新单测，133 passed | `a7dfadb` |
