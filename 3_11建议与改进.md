# 项目综合推进计划（2026-03-22 整合版）

## 当前状态

- **Phase 1** ✅ 完成：TabPFN 基线 82.96%（regime_switching），漂移后恢复 ~61 步
- **Phase 2** ✅ 完成：FastCorrector KNN/EMA 在 regime_switching 上略低于基线（buffer 污染问题）
- **导师反馈**（3月11日）：4 项改进建议，见下文 Phase 2.5

---

## Phase 2.5：导师反馈补强（当前阶段）

在推进 Phase 3 之前，先把 Phase 1 & 2 的实验做扎实。

### 步骤 A：类别平衡检查（10 分钟）

检查三个合成数据集的 0/1 比例，确认实验有效性。

```python
from src.data.synthetic import make_dataset
import numpy as np
for name in ["regime_switching", "rotating_boundary", "combined_drift"]:
    ds = make_dataset(name, n_samples=3000)
    print(f"\n{name}: Overall {np.bincount(ds.y)}")
    for r in sorted(set(ds.regime_labels)):
        mask = ds.regime_labels == r
        print(f"  Regime {r}: {np.bincount(ds.y[mask])}")
```

**若不平衡 > 60:40**：修改 `src/data/synthetic.py` 添加平衡约束，并补充 balanced accuracy / AUC-ROC 指标。

### 步骤 B：多数据集 Phase 2 实验（~2 小时 CPU）

在渐进漂移和混合漂移数据集上跑 Phase 2，验证 FastCorrector 不是"不行"而是"对突变漂移不行"。

```bash
python scripts/run_phase2.py --dataset rotating_boundary --n_samples 3000 --context_size 200
python scripts/run_phase2.py --dataset combined_drift --n_samples 5000 --context_size 200
```

**预期**：`rotating_boundary` 上 KNN/EMA > baseline；`combined_drift` 上部分有效。

**漂移类型对照表**（论文用）：

| 数据集 | 作用于输入特征 P(X) | 作用于关系 P(Y\|X) | 漂移节奏 | 预期 FastCorrector 效果 |
|--------|:-----------------:|:-----------------:|---------|----------------------|
| `rotating_boundary` | ❌ | ✅ 决策边界旋转 | 渐进 | **应该有效** |
| `regime_switching` | ✅ 均值突变 | ✅ 权重突变 | 突变 | 无效（已验证） |
| `combined_drift` | ✅ 均值渐进偏移 | ✅ 权重突变 | 混合 | 部分有效 |

### 步骤 C：Context Window 组合策略（编码 ~2h + 实验 ~1.5h CPU）

将 context window 拆成"固定代表集 + 滑动近期窗"。

**修改文件**：
- `src/data/temporal_loader.py` — 新增 `CompositeWindowLoader`（fixed_pool + sliding_window）
- `scripts/run_baselines.py` — 新增 `--fixed_ratio` 参数

**实验**：
```bash
python scripts/run_baselines.py --dataset regime_switching --context_size 300 --fixed_ratio 0.0   # baseline
python scripts/run_baselines.py --dataset regime_switching --context_size 300 --fixed_ratio 0.93  # 280+20
python scripts/run_baselines.py --dataset regime_switching --context_size 300 --fixed_ratio 0.67  # 200+100
```

**核心指标**：漂移后适应速度，期望 < 当前 61 步。

---

## Phase 3：软门控融合 + 快→中巩固

基于 `implementation_plan_v2.md` 的 Phase 3 设计。

### 步骤 A：GatedEnsemble 实现

**新建文件**：`src/models/gated_ensemble.py`

```python
class GatedEnsemble(nn.Module):
    """软门控融合：根据输入动态分配 slow/inter/fast 三层权重"""
    # gate: x → softmax([α, β, γ])  （α+β+γ=1）
    # adapter: x → 中间层残差校正
    # y_final = α * y_slow + β * y_inter + γ * y_fast
```

设计要点：
- 替代硬体制检测（V1 的 k-means），用软门控自动识别漂移状态
- 参数量 < 10K，CPU 训练无压力
- 稳定时 α 高（信任 TabPFN），漂移时 γ 高（信任快速校正）

### 步骤 B：快→中巩固

**新建文件**：`src/consolidation/fast_to_inter.py`

触发条件：buffer 中最近 N 个校正值方向一致且幅度足够（系统性偏移）。
动作：让 adapter 学习拟合 buffer 中的校正模式 → 清空 buffer。

### 步骤 C：三层集成

**新建文件**：`src/models/multi_timescale.py`

```python
class MultiTimescaleModel:
    def step(self, X_ctx, y_ctx, x_t, y_t):
        y_slow = self.slow_prior.predict(...)    # Level 1
        y_fast = y_slow + self.fast_corrector.correct(x_t)  # Level 3
        y_final, weights = self.gated_ensemble(x, y_slow, y_fast)  # Level 2 + 融合
        # 更新 buffer → 检查巩固触发
```

### 步骤 D：验证

```bash
python scripts/run_phase3.py --dataset regime_switching --n_samples 3000
python scripts/run_phase3.py --dataset rotating_boundary --n_samples 3000
```

**里程碑**：三层系统 > 两层（Level 1+3），门控权重可视化图显示系统自动识别漂移。

---

## Phase 4：完整实验 + 论文

### 步骤 A：实验矩阵

| 模型 | 描述 |
|------|------|
| TabPFN only | Level 1 baseline |
| TabPFN + KNN | Level 1 + 3 (KNN) |
| TabPFN + EMA | Level 1 + 3 (EMA) |
| Ours-Gated | Level 1 + 2 + 3（无巩固）|
| **Ours-Full** | **Level 1 + 2 + 3 + 巩固** |

× 3 个合成数据集 + Electricity 真实数据集

### 步骤 B：真实数据集

实现 `src/data/real_world.py`，加载 Electricity (OpenML #151)。Colab 上跑。

### 步骤 C：消融实验

- buffer_size 敏感性：50, 100, 200
- context 组合比例敏感性
- 巩固阈值敏感性
- 门控权重可视化

### 步骤 D：论文写作

**故事线**：
1. Motivation：TabPFN 面对概念漂移表现退化（Phase 1 图）
2. Core Idea：多时间尺度适应（快速记忆 + 软门控 + 巩固）
3. Method：三层架构 + 数学形式化
4. Experiments：消融实验证明每层贡献；多漂移类型分析；真实数据验证
5. Analysis：门控权重可视化，展示系统如何自动调整策略

**关键图表**：
- 误差 vs 时间曲线（所有模型 × 所有数据集）
- 门控权重 α,β,γ vs 时间
- 漂移类型对照表（含作用对象维度）
- 消融汇总表

---

## 时间线

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | TabPFN 基线 | ✅ 完成 |
| Phase 2 | FastCorrector (regime_switching) | ✅ 完成 |
| **Phase 2.5** | **导师反馈补强**（类别平衡 → 多数据集 → Context 组合）| ⬅️ 当前 |
| Phase 3 | 软门控融合 + 快→中巩固 | 待开始 |
| Phase 4 | 完整实验 + 真实数据 + 论文 | 待开始 |

---

## 关键文件清单

**已有**（需修改）：
- `src/data/temporal_loader.py` — Phase 2.5C 新增 CompositeWindowLoader
- `src/data/synthetic.py` — Phase 2.5A 可能添加平衡约束
- `src/utils/metrics.py` — Phase 2.5A 可能补充 balanced accuracy
- `scripts/run_baselines.py` — Phase 2.5C 新增 --fixed_ratio 参数

**待新建**：
- `src/models/gated_ensemble.py` — Phase 3A
- `src/consolidation/fast_to_inter.py` — Phase 3B
- `src/models/multi_timescale.py` — Phase 3C
- `scripts/run_phase3.py` — Phase 3D
- `src/data/real_world.py` — Phase 4B
